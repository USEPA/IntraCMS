<?php

/**
 * Implements hook_mail_alter().
 */
function maintenance_mail_alter(&$message): void {
  $config = \Drupal::config('maintenance.settings');

  // If the setting is enabled, cancel sending the email.
  if ($config->get('disable_email')) {
    $message['send'] = FALSE;
  }
}
